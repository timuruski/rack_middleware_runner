namespace :middleware do
  desc 'Refresh all middleware repos'
  task :refresh_all do
    # walk vendor directory
    # run git-fetch and git-reset
  end

  desc 'Refresh a middleware repo'
  task :refresh do
    # run git-fetch and git-reset
  end

  desc 'Add a middleware repo'
  task :add do
    # run git-clone
  end
end

desc 'Reload middleware runner'
task :reload do
  # find runner PID
  # send reload signal
end
